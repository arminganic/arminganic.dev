<section class="hero container">
  <div class="hero-content">
    <p class="hero-eyebrow">Full-Stack Engineering · Freelance in Wien</p>
    <h1>Eine Ansprechperson für deine Software – von der Idee bis zum Betrieb.</h1>
    <p class="hero-description">
      Ich plane, entwickle und betreue digitale Produkte für Unternehmen – von der schlanken,
      statischen Webseite bis zur komplexen Anwendung mit eigenem Backend. Ohne Projektmanager oder
      Layer dazwischen: Du sprichst direkt mit der Person, die deinen Code schreibt.
    </p>
  </div>
  <div class="hero-visual">
    <img
      class="hero-image"
      src="/assets/hero-image.jpg"
      alt="Portrait, freundlich lächelnd vor türkisem Hintergrund"
      width="900"
      height="1100"
    />
  </div>
</section>

<style>
  .hero {
    align-items: center;
    display: grid;
    gap: 4rem;
    grid-template-columns: 6fr 4fr;
    min-height: 1000px;
    padding-block: 6rem;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .hero-eyebrow {
    color: var(--pf-text-secondary);
    font-size: var(--pf-font-size-caption);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .hero-description {
    color: var(--pf-text-secondary);
    max-width: 42ch;
  }

  .hero-visual {
    border-radius: 12px;
    height: 100%;
    overflow: hidden;
    position: relative;
  }

  .hero-image {
    height: 100%;
    inset: 0;
    object-fit: cover;
    position: absolute;
    width: 100%;
  }
</style>
