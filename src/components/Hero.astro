<section class="hero container">
  <hgroup>
    <p class="caption">Full-Stack Engineer · Freelancer in Wien</p>
    <h1 class="title">Die Ansprechperson für deine Software – von der Idee bis zum Betrieb.</h1>
    <p class="description">
      Ich plane, entwickle und betreue digitale Produkte für Unternehmen – von dem schlanken,
      statischen Webauftritt bis zur komplexen Anwendung mit eigenem Backend.
    </p>
  </hgroup>
  <div class="visual">
    <img
      class="image"
      src="/assets/hero-image.jpg"
      alt="Portrait, freundlich lächelnd vor türkisem Hintergrund"
      width="900"
      height="1100"
    />
  </div>
</section>

<style>
  .caption {
    color: var(--pf-text-secondary);
    font-size: var(--pf-font-size-caption);
    font-weight: 500;
    letter-spacing: 0.04em;
    margin-block-end: 1rem;
    text-transform: uppercase;
  }

  .title {
    margin-block-end: 1.5rem;
  }

  .description {
    color: var(--pf-text-secondary);
    max-width: 42ch;
  }

  .visual {
    overflow: hidden;
    position: relative;
  }

  .image {
    height: 100%;
    inset: 0;
    object-fit: cover;
    position: absolute;
    width: 100%;
  }

  @media screen and (width < 768px) {
    .hero {
      display: flex;
      flex-direction: column;
      padding-block: 3rem;
    }

    .description {
      margin-block-end: 3rem;
    }

    .visual {
      align-self: center;
      aspect-ratio: 1;
      border-radius: 9999px;
      width: 60%;
    }

    .image {
      object-position: 0 20%;
    }
  }

  @media screen and (width >= 768px) {
    .hero {
      align-items: center;
      column-gap: 4rem;
      display: grid;
      grid-template-columns: 6fr 4fr;
      min-height: 900px;
      padding-block: 6rem;
    }

    .visual {
      border-radius: 12px;
      height: 100%;
    }
  }
</style>
